# AudioSet unbal_train — audio-only tokenization.

output_name: audioset_unbal_train

# Pre-built Shar directory
shar_dir: /iopsstor/scratch/cscs/xyixuan/audio-datasets/audioset_unbal_train_shar

min_duration: 2.0
max_batch_duration: 2500.0
# DataLoader (72 CPUs per rank on GH200)
num_workers: 64
checkpoint_interval_batches: 1000
